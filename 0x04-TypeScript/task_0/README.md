# Task 0: Creating an Interface for a Student

## Overview

This project demonstrates basic TypeScript usage:

- Defining an interface
- Creating typed objects
- Storing objects in a typed array
- Rendering data to the DOM using Vanilla JavaScript

---

## Project Structure


---

## Steps to Run

1. Open a terminal in the `task_0/` directory.
2. Install dependencies:

```bash
npm install

npm run start-dev
